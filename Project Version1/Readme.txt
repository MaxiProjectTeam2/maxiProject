This is VERSION 4A code. NOW REDUNDANT. DO NOT USE

The database is created in the database folder. this database is empty,so you can add any records in it.
During the Easter, please finish the php code, add it to the html file and test it. Make sure the php code can works well in the html page.
After the Easter, we will combine all the page and test the whole projec.
Happy coding!
